export interface IUser {
    title: string
    avatarUrl: string
    index: number
}

export interface IRepository {
    title: string
    avatarUrl: string
    index: number
    description: string
    created_at: string
    stargazers_count: number
    forks_count: number
}
